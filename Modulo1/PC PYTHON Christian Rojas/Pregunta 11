def main():
    lista_primera = [1, 1, 2, 3, 4, 4, 5, 1]
    lista_sin_numero_repetidos = list(set(lista_primera))
    
    print("Lista primera:", lista_primera)
    print("Lista procesada:", lista_sin_numero_repetidos)

if __name__ == "__main__":
    main()